<!--Мёртвый Космос, Licensed under custom terms with restrictions on public hosting and commercial use, full text: https://raw.githubusercontent.com/dead-space-server/space-station-14-fobos/master/LICENSE.TXT-->

<DefaultWindow xmlns="https://spacestation14.io"
               Title="{Loc 'virus-evolution-window-title'}"
               MinSize="900,600"
               SetSize="1000,700">

    <TabContainer Name="MasterTabContainer">

        <!-- ================= EVOLUTION TAB ================= -->
        <BoxContainer Name="EvolutionTab"
                      Orientation="Vertical"
                      HorizontalExpand="True"
                      VerticalExpand="True">

            <!-- CONTENTS -->
            <PanelContainer Name="EvolutionContents"
                            StyleClasses="Inset"
                            VerticalExpand="True"
                            Margin="5">

                <BoxContainer Orientation="Horizontal"
                              HorizontalExpand="True"
                              VerticalExpand="True">

                    <!-- Available symptoms -->
                    <PanelContainer MinWidth="300" Margin="5">
                        <BoxContainer Orientation="Vertical">
                            <Label Text="{Loc 'virus-evolution-available-symptoms'}" />
                            <ScrollContainer VerticalExpand="True">
                                <ItemList Name="AvailableSymptomsList"
                                          Access="Public"
                                          SelectMode="Single" />
                            </ScrollContainer>
                        </BoxContainer>
                    </PanelContainer>

                    <!-- Center -->
                    <PanelContainer Margin="5"
                                    HorizontalExpand="True"
                                    VerticalExpand="True">
                        <BoxContainer Orientation="Vertical"
                                      VerticalExpand="True">
                            <!-- Статистика вируса -->
                            <BoxContainer Orientation="Vertical" Margin="0 0 0 10">
                                <Label Name="VirusHealthLabel" StyleClasses="LabelSmall"/>
                                <Label Name="InfectivityLabel" StyleClasses="LabelSmall"/>
                                <Label Name="InfectedCountLabel" StyleClasses="LabelSmall"/>
                                <Label Name="PointsPerSecondLabel" StyleClasses="LabelSmall"/>
                            </BoxContainer>

                            <Label Text="{Loc 'virus-evolution-description-header'}" />
                            <BoxContainer Orientation="Horizontal"
                                        HorizontalAlignment="Left">
                                <RichTextLabel Name="SymptomDescription"
                                            MinWidth="300"
                                            MaxWidth="300"
                                            Margin="5"/>
                            </BoxContainer>


                            <Label Name="WhitelistMutationPointsLabel"/>
                            <Label Name="SymptomCostLabel"/>

                            <Button Name="BuySymptomButton"
                                    Access="Public"
                                    Text="{Loc 'virus-evolution-buy-button'}"
                                    Disabled="True"/>

                            <Button Name="DeleteSymptomButton"
                                    Access="Public"
                                    Text="{Loc 'virus-evolution-delete-button'}"
                                    Disabled="True"/>
                        </BoxContainer>
                    </PanelContainer>

                    <!-- Active symptoms -->
                    <PanelContainer MinWidth="300" Margin="5">
                        <BoxContainer Orientation="Vertical">
                            <Label Text="{Loc 'virus-evolution-active-symptoms'}" />
                            <ScrollContainer VerticalExpand="True">
                                <ItemList Name="ActiveSymptomsList"
                                          Access="Public"
                                          SelectMode="Single" />
                            </ScrollContainer>
                        </BoxContainer>
                    </PanelContainer>

                </BoxContainer>
            </PanelContainer>

            <!-- DATASERVER MISSING -->
            <PanelContainer Name="EvolutionServerMissing"
                            StyleClasses="Inset"
                            VerticalExpand="True"
                            Margin="5">
                <Label HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Text="{Loc 'virus-evolution-dataserver-missing'}"/>
            </PanelContainer>

            <!-- !HASVIRUS -->
            <PanelContainer Name="VirusDataMissing"
                            StyleClasses="Inset"
                            VerticalExpand="True"
                            Margin="5">
                <Label HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Text="{Loc 'virus-evolution-virusdata-missing'}"/>
            </PanelContainer>

            <!-- DATASERVER FAR -->
            <PanelContainer Name="EvolutionServerFar"
                            StyleClasses="Inset"
                            VerticalExpand="True"
                            Margin="5">
                <Label HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Text="{Loc 'virus-evolution-dataserver-far'}"/>
            </PanelContainer>

        </BoxContainer>
        <BoxContainer Name="NecroTab"
                    Orientation="Vertical"
                    VerticalExpand="True">
                    <BoxContainer Orientation="Vertical" HorizontalExpand="True" VerticalExpand="True">
                        <!-- Первый слайдер -->
                        <BoxContainer Orientation="Horizontal" HorizontalExpand="True" Margin="5">
                            <Label Text="Health Multiplier:" VerticalAlignment="Center"/>
                            <Slider Name="HpMulty" HorizontalExpand="True" Value = "1" MaxValue="10" MinValue="0.1" RoundingDecimals = "2" Rounded = "True"/>
                            <Label Name="HpMulty_Value" Text = "1" VerticalAlignment="Center" />
                        </BoxContainer>

                        <!-- Второй слайдер -->
                        <BoxContainer Orientation="Horizontal" HorizontalExpand="True" Margin="5">
                            <Label Text="Damage Multiplier:" VerticalAlignment="Center"/>
                            <Slider Name="DamageMulty" HorizontalExpand="True"  Value = "1" MaxValue="10" MinValue="0.1" RoundingDecimals = "2" Rounded = "True" />
                            <Label Name="DamageMulty_Value" Text = "1" VerticalAlignment="Center" />
                        </BoxContainer>

                        <!-- Третий слайдер -->
                        <BoxContainer Orientation="Horizontal" HorizontalExpand="True" Margin="5">
                            <Label Text="Speed Multiplier:" VerticalAlignment="Center"/>
                            <Slider Name="SpeedMulty" HorizontalExpand="True"  Value = "1" MaxValue="10" MinValue="0.1" RoundingDecimals = "2" Rounded = "True" />
                            <Label Name="SpeedMulty_Value" Text = "1" VerticalAlignment="Center" />
                        </BoxContainer>

                        <!-- Четвертый слайдер -->
                        <BoxContainer Orientation="Horizontal" HorizontalExpand="True" Margin="5">
                            <Label Text="Stamina Multiplier:" VerticalAlignment="Center"/>
                            <Slider Name="StaminaMulty" HorizontalExpand="True"  Value = "1" MaxValue="10" MinValue="0.1" RoundingDecimals = "2" Rounded = "True" />
                            <Label Name="StaminaMulty_Value" Text = "1" VerticalAlignment="Center" />
                        </BoxContainer>
                        <Label Name="FinalPriceLabel" Text="Final Price: 0" Margin="10" HorizontalAlignment="Center"/>
                    </BoxContainer>
                    <Button Name="ChangeNecroVirus"
                                    Access="Public"
                                    Text="{Loc 'virus-necro-change-button'}"
                                    Disabled="False"/>
        </BoxContainer>
        <!-- ================= WHITELIST TAB ================= -->
        <BoxContainer Name="WhitelistTab"
                    Orientation="Vertical"
                    VerticalExpand="True">

            <PanelContainer Name="WhitelistContents"
                            StyleClasses="Inset"
                            VerticalExpand="True"
                            Margin="5">

                <BoxContainer Orientation="Horizontal"
                            HorizontalExpand="True"
                            VerticalExpand="True">

                    <!-- Available bodies -->
                    <PanelContainer MinWidth="220" Margin="5">
                        <BoxContainer Orientation="Vertical">
                            <Label Text="{Loc 'virus-evolution-available-bodies'}"/>
                            <ScrollContainer VerticalExpand="True">
                                <ItemList Name="AvailableBodiesList"
                                        Access="Public"
                                        SelectMode="Single"/>
                            </ScrollContainer>
                        </BoxContainer>
                    </PanelContainer>

                    <!-- Center -->
                    <PanelContainer Margin="5"
                                    HorizontalExpand="True"
                                    VerticalExpand="True">
                        <BoxContainer Orientation="Vertical">
                            <Label Name="MutationPointsLabel"/>
                            <Label Name="BodyCostLabel"/>

                            <Button Name="BuyBodyButton"
                                    Access="Public"
                                    Text="{Loc 'virus-evolution-buy-body'}"
                                    Disabled="True"/>
                            
                            <Button Name="DeleteBodyButton"
                                    Access="Public"
                                    Text="{Loc 'virus-evolution-delete-body'}"
                                    Disabled="True"/>
                        </BoxContainer>
                    </PanelContainer>

                    <!-- Active bodies -->
                    <PanelContainer MinWidth="220" Margin="5">
                        <BoxContainer Orientation="Vertical">
                            <Label Text="{Loc 'virus-evolution-active-bodies'}"/>
                            <ScrollContainer VerticalExpand="True">
                                <ItemList Name="ActiveBodiesList"
                                        Access="Public"
                                        SelectMode="Single"/>
                            </ScrollContainer>
                        </BoxContainer>
                    </PanelContainer>

                </BoxContainer>
            </PanelContainer>

            <!-- SERVER MISSING / FAR — без изменений -->
        </BoxContainer>
    </TabContainer>
</DefaultWindow>

